http:
  routers:
    openclaw:
      rule: "Host(`openclaw.scalex.money`)"
      service: openclaw
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

  services:
    openclaw:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:18789"
